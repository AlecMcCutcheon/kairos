# Contracts

WASM time-oracle crate will live here (`kairos-time`).

Until then:

- State machine: [`../SCHEMA.md`](../SCHEMA.md)
- UI demos seal stamps in-browser only (`site/app.js` → `simulateStamp`)

Suggested layout later:

```
contracts/kairos-time/   # Rust → wasm32 contract
contracts/build.sh
```
